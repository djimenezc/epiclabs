buildscript {
  repositories {
    mavenCentral()
  }

  dependencies {
    classpath group: "net.carlosgsouza", name: "gradle-console", version: "1.+"
  }
}

apply plugin: "java"
apply plugin: "application"
apply plugin: "checkstyle"
apply plugin: "console"

repositories {
  mavenCentral()
}

dependencies {
  compile group: 'com.google.code.externalsortinginjava', name: 'externalsortinginjava', version: '[0.1.9,)'
  testCompile group: "junit", name: "junit", version: "4.+"
}

//mainClassName = "com.djimenezc.Main"
